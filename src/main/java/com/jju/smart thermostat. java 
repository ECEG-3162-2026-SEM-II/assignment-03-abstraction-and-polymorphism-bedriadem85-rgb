public class SmartThermostat extends SmartDevice {

    // Constructor
    public Smart Thermostat(String name) {
        super(name);
    }

    // Override activate method
    @Override
    public void activate() {
        System.out.println(getName() + " is setting temperature to 72 degrees.");
    }
}